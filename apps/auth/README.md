# Auth Service

Minimal authentication service scaffold. It currently exposes health endpoints only. Authentication behavior, persistence, sessions, and email are added through approved OpenSpec changes.

## Local

```bash
npm install
npm run dev
```

Default port: `8083`.
